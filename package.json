{
  "name": "notejs-remake",
  "version": "0.2.0",
  "description": "A remake of the original didn't work at all notejs",
  "repository": {
    "type": "git",
    "url": "https://github.com/shari09/notejs-remake.git"
  },
  "dependencies": {
  },
  "devDependencies": {
    "dotenv": "~8.2.0",
    "nodemon": "~2.0.2"
  },
  "scripts": {
    "postinstall": "cd backend/ && npm install && cd ../extension/ && npm install",
    "start": "cd backend/ && npm start",
    "test": "standard",
    "build": "cd extension/ && npm run build"
  }
}